# Suppor Vector Machine (SVM)
Support Vector Machine (SVM) is a supervised machine learning algorithm used for both classification and regression tasks. It works by finding the optimal boundary (hyperplane) that separates data points into different classes.

### Types:
- Support Vector Classification
- Support Vector Regression.